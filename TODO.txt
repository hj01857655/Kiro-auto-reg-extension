================================================================================
                    KIRO ACCOUNT SWITCHER - АРХИТЕКТУРНЫЙ АУДИТ
                              TODO & ROADMAP
================================================================================

Дата аудита: 14.12.2024
Версия: 5.1.0

================================================================================
                         🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ
================================================================================

┌─────────────────────────────────────────────────────────────────────────────┐
│ 1. ХАРДКОД СТРОК В КОДЕ (несмотря на наличие i18n системы)                 │
└─────────────────────────────────────────────────────────────────────────────┘

ПРОБЛЕМА:
  Система локализации существует (src/webview/i18n/), но многие строки 
  захардкожены прямо в коде.

ФАЙЛЫ С ХАРДКОДОМ:

  ▸ src/webview/index.ts (строки 130-160)
    - "Active", "Ready", "Expired / Exhausted" - группы аккаунтов
    - "Back" - кнопка назад
    - Тексты патча Kiro
    
  ▸ src/webview/components/ProfileEditor.ts (весь файл)
    - Все строки на русском без i18n
    - PROVIDERS объект с русскими описаниями
    - STRATEGY_DIAGRAMS с русскими текстами
    
  ▸ src/webview/scripts.ts (строки 90-300)
    - Inline объекты { en: '...', ru: '...' } вместо i18n
    - Дублирование переводов
    
  ▸ src/providers/AccountsProvider.ts
    - Сообщения vscode.window.showInformationMessage на английском

КАК ИСПРАВИТЬ:

  Шаг 1: Добавить недостающие ключи в src/webview/i18n/types.ts
  ─────────────────────────────────────────────────────────────
  
  export interface Translations {
    // ... существующие ключи ...
    
    // Account Groups
    activeGroup: string;
    readyGroup: string;
    badGroup: string;
    
    // Navigation
    back: string;
    
    // Patch Section
    kiroPatch: string;
    kiroPatchDesc: string;
    patchStatusLoading: string;
    patchStatusActive: string;
    patchStatusNotPatched: string;
    patch: string;
    removePatch: string;
    newId: string;
    
    // Profile Editor (все строки)
    enterYourEmail: string;
    detected: string;
    chooseStrategy: string;
    recommended: string;
    otherOptions: string;
    // ... и т.д.
  }

  Шаг 2: Добавить переводы во все файлы локалей
  ─────────────────────────────────────────────
  
  Обновить все 10 файлов в src/webview/i18n/locales/:
  - en.ts, ru.ts, zh.ts, es.ts, pt.ts, ja.ts, de.ts, fr.ts, ko.ts, hi.ts

  Шаг 3: Заменить хардкод на вызовы t.key
  ───────────────────────────────────────
  
  // БЫЛО:
  html += `<div class="list-group"><span>Active</span>`;
  
  // СТАЛО:
  html += `<div class="list-group"><span>${t.activeGroup}</span>`;

  Шаг 4: Передать translations в scripts.ts
  ─────────────────────────────────────────
  
  // В generateWebviewScript добавить параметр translations
  export function generateWebviewScript(totalAccounts: number, t: Translations): string {
    return `
      const T = ${JSON.stringify(t)};
      // Использовать T.key вместо хардкода
    `;
  }


┌─────────────────────────────────────────────────────────────────────────────┐
│ 2. ProfileEditor.ts - ПОЛНОСТЬЮ НА РУССКОМ                                 │
└─────────────────────────────────────────────────────────────────────────────┘

ПРОБЛЕМА:
  Компонент ProfileEditor содержит ~750 строк с русским текстом без 
  использования системы локализации.

КАК ИСПРАВИТЬ:

  Вариант A (Рекомендуемый): Рефакторинг с i18n
  ─────────────────────────────────────────────
  
  1. Добавить все ключи ProfileEditor в Translations interface
  2. Передавать объект переводов в renderProfileEditor()
  3. Заменить все строки на t.key
  
  // БЫЛО:
  export function renderProfileEditor({ profile, providerHints, mode }: ProfileEditorProps): string {
  
  // СТАЛО:
  export function renderProfileEditor({ 
    profile, 
    providerHints, 
    mode,
    translations  // <-- добавить
  }: ProfileEditorProps): string {

  Вариант B: Отдельный файл локализации для ProfileEditor
  ───────────────────────────────────────────────────────
  
  Создать src/webview/i18n/profile-editor.ts с переводами только для этого
  компонента. Менее предпочтительно - нарушает единообразие.


================================================================================
                         🟡 АРХИТЕКТУРНЫЕ ПРОБЛЕМЫ
================================================================================

┌─────────────────────────────────────────────────────────────────────────────┐
│ 3. AccountsProvider.ts - GOD OBJECT (700+ строк)                           │
└─────────────────────────────────────────────────────────────────────────────┘

ПРОБЛЕМА:
  Класс делает слишком много:
  - Управление webview
  - Загрузка/сохранение аккаунтов
  - IMAP профили
  - Логирование
  - Обновления
  - Патчинг Kiro

КАК ИСПРАВИТЬ:

  Шаг 1: Выделить LogService
  ──────────────────────────
  
  Создать: src/services/LogService.ts
  
  export class LogService {
    private logs: string[] = [];
    private logFile: string;
    
    constructor(logDir: string) {
      this.logFile = path.join(logDir, 'autoreg.log');
    }
    
    add(message: string): void { ... }
    clear(): void { ... }
    getAll(): string[] { ... }
    writeToFile(line: string): void { ... }
  }

  Шаг 2: Выделить ProfileService (уже частично есть ImapProfileProvider)
  ─────────────────────────────────────────────────────────────────────
  
  Переместить всю логику профилей из AccountsProvider в ImapProfileProvider:
  - loadProfiles()
  - createProfile()
  - updateProfile()
  - deleteProfile()
  - setActiveProfile()

  Шаг 3: Выделить UsageService
  ────────────────────────────
  
  Создать: src/services/UsageService.ts
  
  export class UsageService {
    async refreshUsage(): Promise<KiroUsageData | null> { ... }
    async refreshUsageAfterSwitch(): Promise<void> { ... }
    getCached(accountName: string): KiroUsageData | null { ... }
    invalidate(accountName: string): void { ... }
  }

  Шаг 4: Упростить AccountsProvider
  ─────────────────────────────────
  
  После рефакторинга AccountsProvider должен только:
  - Управлять webview lifecycle
  - Делегировать команды соответствующим сервисам
  - Рендерить HTML


┌─────────────────────────────────────────────────────────────────────────────┐
│ 4. ДУБЛИРОВАНИЕ ЛОГИКИ ПЕРЕВОДОВ В scripts.ts                              │
└─────────────────────────────────────────────────────────────────────────────┘

ПРОБЛЕМА:
  В scripts.ts создаются inline объекты переводов:
  
  const titles = { en: 'Delete Account', ru: 'Удалить аккаунт' };
  const texts = { en: 'Are you sure?', ru: 'Вы уверены?' };

КАК ИСПРАВИТЬ:

  Передавать полный объект переводов в скрипт:
  
  // В index.ts при генерации HTML:
  const script = generateWebviewScript(accounts.length, t);
  
  // В scripts.ts:
  export function generateWebviewScript(totalAccounts: number, t: Translations): string {
    return `
      const T = ${JSON.stringify(t)};
      
      function confirmDelete(filename) {
        document.getElementById('dialogTitle').textContent = T.deleteTitle;
        document.getElementById('dialogText').textContent = T.deleteConfirm;
        // ...
      }
    `;
  }


┌─────────────────────────────────────────────────────────────────────────────┐
│ 5. ОТСУТСТВИЕ ВАЛИДАЦИИ ПОЛНОТЫ ПЕРЕВОДОВ                                  │
└─────────────────────────────────────────────────────────────────────────────┘

ПРОБЛЕМА:
  Нет проверки что все ключи переведены во всех языках.
  Можно добавить ключ в en.ts и забыть про остальные 9 файлов.

КАК ИСПРАВИТЬ:

  Вариант A: TypeScript strict проверка
  ─────────────────────────────────────
  
  // В каждом файле локали:
  export const ru: Translations = { ... }  // TypeScript проверит все ключи
  
  Уже реализовано! Но нужно убедиться что все файлы используют тип.

  Вариант B: Runtime проверка при разработке
  ──────────────────────────────────────────
  
  // В i18n/index.ts добавить:
  if (process.env.NODE_ENV === 'development') {
    const enKeys = Object.keys(en);
    for (const [lang, trans] of Object.entries(translations)) {
      const missing = enKeys.filter(k => !(k in trans));
      if (missing.length > 0) {
        console.warn(`Missing keys in ${lang}:`, missing);
      }
    }
  }

  Вариант C: Тест
  ───────────────
  
  // tests/i18n.test.ts
  describe('i18n completeness', () => {
    const enKeys = Object.keys(en);
    
    Object.entries(translations).forEach(([lang, trans]) => {
      it(`${lang} should have all keys`, () => {
        const missing = enKeys.filter(k => !(k in trans));
        expect(missing).toEqual([]);
      });
    });
  });


================================================================================
                         🟢 УЛУЧШЕНИЯ (NICE TO HAVE)
================================================================================

┌─────────────────────────────────────────────────────────────────────────────┐
│ 6. СТРУКТУРА ФАЙЛОВ СТИЛЕЙ                                                 │
└─────────────────────────────────────────────────────────────────────────────┘

ТЕКУЩАЯ СТРУКТУРА (хорошая):
  src/webview/styles/
  ├── index.ts      # Экспорт всех стилей
  ├── variables.ts  # CSS переменные
  ├── base.ts       # Базовые стили
  ├── components.ts # Компоненты
  ├── layout.ts     # Лейаут
  └── profiles.ts   # Стили профилей

РЕКОМЕНДАЦИЯ:
  Структура хорошая. Можно добавить:
  - animations.ts - для анимаций
  - themes.ts - для поддержки тем (light/dark)


┌─────────────────────────────────────────────────────────────────────────────┐
│ 7. ТИПИЗАЦИЯ СООБЩЕНИЙ WEBVIEW                                             │
└─────────────────────────────────────────────────────────────────────────────┘

ПРОБЛЕМА:
  Сообщения между webview и extension не типизированы:
  
  vscode.postMessage({ command: 'switchAccount', email: filename });

КАК ИСПРАВИТЬ:

  Создать: src/webview/messages.ts
  
  export type WebviewMessage = 
    | { command: 'switchAccount'; email: string }
    | { command: 'deleteAccount'; email: string }
    | { command: 'refresh' }
    | { command: 'createProfile'; profile: Partial<ImapProfile> }
    // ... все команды
  
  export type ExtensionMessage =
    | { type: 'appendLog'; log: string }
    | { type: 'updateStatus'; status: string }
    | { type: 'profilesLoaded'; profiles: ImapProfile[]; activeProfileId?: string }
    // ... все сообщения


┌─────────────────────────────────────────────────────────────────────────────┐
│ 8. КОНСТАНТЫ И КОНФИГУРАЦИЯ                                                │
└─────────────────────────────────────────────────────────────────────────────┘

ПРОБЛЕМА:
  Magic numbers и строки разбросаны по коду:
  
  if (this._consoleLogs.length > 200) { ... }
  const CACHE_TTL = 5 * 60 * 1000;

КАК ИСПРАВИТЬ:

  Создать: src/constants.ts
  
  export const CONFIG = {
    MAX_CONSOLE_LOGS: 200,
    CACHE_TTL_MS: 5 * 60 * 1000,
    RENDER_DEBOUNCE_MS: 50,
    USAGE_REFRESH_RETRIES: 3,
    USAGE_REFRESH_DELAYS: [500, 1000, 2000],
  } as const;


================================================================================
                         📋 ПОРЯДОК ВЫПОЛНЕНИЯ
================================================================================

ФАЗА 1: Локализация (приоритет: ВЫСОКИЙ)
────────────────────────────────────────
[✓] 1.1 Добавить все недостающие ключи в types.ts (60+ новых ключей)
[✓] 1.2 Обновить en.ts с новыми ключами
[✓] 1.3 Обновить ru.ts с новыми ключами
[✓] 1.4 Обновить остальные 8 локалей (zh, es, de, fr, pt, ja, ko, hi)
[✓] 1.5 Исправить хардкод в index.ts (группы аккаунтов, патч, кнопки)
[✓] 1.6 Передать translations в scripts.ts (T объект на клиенте)
[✓] 1.7 Рефакторинг ProfileEditor.ts с i18n (полная переработка)

ФАЗА 2: Архитектура (приоритет: СРЕДНИЙ)
────────────────────────────────────────
[✓] 2.1 Создать src/services/LogService.ts
[✓] 2.2 Создать src/services/UsageService.ts
[✓] 2.3 Рефакторинг AccountsProvider.ts (интеграция сервисов)
[✓] 2.4 Типизация сообщений webview (src/webview/messages.ts)

ФАЗА 3: Качество кода (приоритет: НИЗКИЙ)
─────────────────────────────────────────
[✓] 3.1 Создать src/constants.ts
[✓] 3.2 Добавить тест на полноту переводов (уже есть в extension-paths.test.ts)
[ ] 3.3 Документация архитектуры


================================================================================
                         📁 ЦЕЛЕВАЯ СТРУКТУРА ПРОЕКТА
================================================================================

src/
├── commands/
│   ├── autoreg.ts
│   ├── index.ts
│   └── webview-handler.ts
│
├── providers/
│   ├── AccountsProvider.ts    # Упрощённый, только webview
│   ├── ImapProfileProvider.ts
│   └── index.ts
│
├── services/                   # НОВАЯ ПАПКА
│   ├── LogService.ts
│   ├── UsageService.ts
│   └── index.ts
│
├── state/
│   ├── StateManager.ts
│   └── index.ts
│
├── webview/
│   ├── components/
│   │   ├── AccountCard.ts
│   │   ├── ConsolePanel.ts
│   │   ├── ImapProfileCard.ts
│   │   ├── ProfileEditor.ts   # С i18n
│   │   ├── ProgressPanel.ts
│   │   ├── SettingsPanel.ts
│   │   ├── UsageCard.ts
│   │   └── index.ts
│   │
│   ├── i18n/
│   │   ├── locales/
│   │   │   ├── de.ts
│   │   │   ├── en.ts          # Полный
│   │   │   ├── es.ts
│   │   │   ├── fr.ts
│   │   │   ├── hi.ts
│   │   │   ├── ja.ts
│   │   │   ├── ko.ts
│   │   │   ├── pt.ts
│   │   │   ├── ru.ts          # Полный
│   │   │   └── zh.ts
│   │   ├── index.ts
│   │   └── types.ts           # Расширенный
│   │
│   ├── styles/
│   │   ├── base.ts
│   │   ├── components.ts
│   │   ├── index.ts
│   │   ├── layout.ts
│   │   ├── profiles.ts
│   │   └── variables.ts
│   │
│   ├── helpers.ts
│   ├── icons.ts
│   ├── index.ts
│   ├── messages.ts            # НОВЫЙ - типы сообщений
│   ├── scripts.ts             # С переданными translations
│   ├── state.ts
│   ├── types.ts
│   └── virtualList.ts
│
├── accounts.ts
├── constants.ts               # НОВЫЙ
├── extension.ts
├── kiro-commands.ts
├── process-manager.ts
├── types.ts
├── update-checker.ts
├── utils.ts
└── webview.ts


================================================================================
                              КОНЕЦ ДОКУМЕНТА
================================================================================
